name: Supabase Keep Alive

on:
  schedule:
    - cron: "0 12 */3 * *"
  workflow_dispatch:

jobs:
  keepalive:
    runs-on: ubuntu-latest

    steps:
      - name: Ping Supabase Auth
        run: |
          curl -I "https://qjicwqpjxsqynoudwylk.supabase.co/auth/v1/health"
