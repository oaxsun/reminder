# Korah

Versión limpia para GitHub Pages.

Estructura usada por `index.html`:

- `css/styles.css`
- `js/app.js`
- `assets/`
- `database/schema.sql`

No incluye duplicados en raíz como `app.js` o `schema.sql`.
